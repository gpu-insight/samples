import os
Import('env')

glut_env = env.Clone()
glut_env.Append(LIBS=['glut'])

source_list = glut_env.Glob('*.cpp')

executables = []
for src in source_list:
    executables = glut_env.Program(src)

glut_env.Alias('install', glut_env.Install(glut_env['BINDIR'], executables))
